# Android build output
bin/
.buildozer/

# Python cache
__pycache__/
*.pyc
*.pyo

# ── SENSITIVE — never commit these ────────────────────────────────────────
# Keystore and credentials — back these up somewhere safe offline
release.keystore
.keystore.env
key.txt
*.keystore
*.jks
